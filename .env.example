# Database connection string (defaults to local SQLite file:./local.db if left empty)
DATABASE_URL=file:./local.db

# JWT session secret key for staff authentication (minimum 32 characters)
JWT_SECRET=civic-resolve-super-secret-key-must-be-32-chars-long-2026

# Server Port (optional, defaults to 3000)
PORT=3000

